import { css } from '@emotion/react';

export const ContactsList = ()=> {
  return(
    <div></div>
    // <ul className={css.list}>
    //   {tasks.map(({ id, text }) => (
    //     <li key={id}>
    //       <Task id={id} text={text} />
    //     </li>
    //   ))}
    // </ul>
  )
}
